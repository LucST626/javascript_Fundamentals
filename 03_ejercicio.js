var usuario = {
    nombre: "Lucas",
    edad: 30, 
    ciudad: "Granada"
  };
  
  var saludo = `Hola, me llamo ${usuario.nombre}, tengo ${usuario.edad} años y soy de ${usuario.ciudad}.`;
  console.log(saludo);
