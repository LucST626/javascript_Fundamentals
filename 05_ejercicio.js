// Definir la función areaCirculo
function areaCirculo(radio) {
    var area = Math.PI * radio * radio;
    console.log("El área del círculo con radio " + radio + " es: " + area);
    return area;
  }
  
  // Llamar función con valor  radio específico
  areaCirculo(5); 